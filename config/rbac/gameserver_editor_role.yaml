# This rule is not used by the project kterodactyl itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the game.kterodactyl.io.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: kterodactyl
    app.kubernetes.io/managed-by: kustomize
  name: gameserver-editor-role
rules:
- apiGroups:
  - game.kterodactyl.io
  resources:
  - gameservers
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - game.kterodactyl.io
  resources:
  - gameservers/status
  verbs:
  - get
