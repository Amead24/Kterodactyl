apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "kterodactyl.fullname" . }}-leader-election-rolebinding
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kterodactyl.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "kterodactyl.fullname" . }}-leader-election-role
subjects:
- kind: ServiceAccount
  name: {{ include "kterodactyl.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
