---
phase: 02-networking-dns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - internal/util/networking.go
  - internal/util/labels.go
  - config/manager/admin-config.yaml
  - cmd/main.go
autonomous: true

must_haves:
  truths:
    - "Gateway API Go types are importable in the project"
    - "DNS name construction follows the pattern game.username.baseDomain"
    - "Admin ConfigMap includes baseDomain, gatewayName, and gatewayNamespace fields"
  artifacts:
    - path: "internal/util/networking.go"
      provides: "DNS name construction utility and networking constants"
      exports: ["GameServerDNSName", "AnnotationDNSName", "LabelHTTPRouteOwner"]
    - path: "internal/util/labels.go"
      provides: "Updated label constants with networking additions"
      contains: "AnnotationDNSName"
    - path: "config/manager/admin-config.yaml"
      provides: "Admin ConfigMap with networking configuration fields"
      contains: "baseDomain"
    - path: "cmd/main.go"
      provides: "Gateway API scheme registration"
      contains: "gatewayv1.Install"
  key_links:
    - from: "internal/util/networking.go"
      to: "internal/util/labels.go"
      via: "shared constants package"
      pattern: "package util"
    - from: "cmd/main.go"
      to: "sigs.k8s.io/gateway-api/apis/v1"
      via: "scheme registration in init()"
      pattern: "gatewayv1\\.Install\\(scheme\\)"
---

<objective>
Add Gateway API dependency, networking utilities, and admin config extensions for DNS routing.

Purpose: Phase 2 requires Gateway API types (HTTPRoute) and centralized DNS name construction. This plan adds the Go module dependency, registers Gateway API types with the scheme, creates networking utility functions, and extends the admin ConfigMap with baseDomain/gatewayName/gatewayNamespace fields needed by the DNS controller in Plan 02.

Output: Gateway API importable, `GameServerDNSName()` utility available, admin config extended with networking fields, scheme registered for HTTPRoute serialization.
</objective>

<execution_context>
@/home/tony/.claude/get-shit-done/workflows/execute-plan.md
@/home/tony/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-networking-dns/02-RESEARCH.md
@internal/util/labels.go
@cmd/main.go
@go.mod
@config/manager/admin-config.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Gateway API dependency and create networking utilities</name>
  <files>
    go.mod
    go.sum
    internal/util/networking.go
    internal/util/labels.go
  </files>
  <action>
1. Add the Gateway API Go module dependency:
   ```
   cd /home/tony/kterodactyl && go get sigs.k8s.io/gateway-api@v1.4.1
   ```

2. Create `internal/util/networking.go` with:
   - `GameServerDNSName(gameType, owner, baseDomain string) string` that returns `fmt.Sprintf("%s.%s.%s", gameType, owner, baseDomain)` following the pattern `game.username.domain.com`
   - Networking-related annotation constants:
     - `AnnotationDNSName = "kterodactyl.io/dns-name"` (stores computed DNS name)
     - `AnnotationExternalDNSTTL = "external-dns.alpha.kubernetes.io/ttl"` (ExternalDNS TTL annotation)
   - Networking-related label constants:
     - `LabelHTTPRouteOwner = "kterodactyl.io/gameserver"` (links HTTPRoute back to GameServer)
   - Use `package util` to match existing labels.go

3. Move networking-related annotations and labels OUT of labels.go and INTO networking.go. Keep labels.go focused on existing label/annotation constants (LabelOwner, LabelGame, LabelUser, LabelManagedBy, AnnotationAllocated, etc.). The networking.go file owns all networking-specific constants.

4. Run `go build ./...` to verify compilation succeeds with the new dependency.

Do NOT modify any controller code in this task. Only utilities and dependencies.
  </action>
  <verify>
    `cd /home/tony/kterodactyl && go build ./...` succeeds.
    `grep "gateway-api" go.mod` shows the dependency.
    File `internal/util/networking.go` exists and contains `GameServerDNSName`.
  </verify>
  <done>
    Gateway API v1.4.1 in go.mod, networking.go exports GameServerDNSName and networking constants, go build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register Gateway API scheme and extend admin ConfigMap</name>
  <files>
    cmd/main.go
    config/manager/admin-config.yaml
  </files>
  <action>
1. Update `cmd/main.go`:
   - Add import: `gatewayv1 "sigs.k8s.io/gateway-api/apis/v1"`
   - In the `init()` function, add after the existing scheme registrations:
     `utilruntime.Must(gatewayv1.Install(scheme))`
   - This enables the controller manager to serialize/deserialize HTTPRoute and Gateway resources.

2. Update `config/manager/admin-config.yaml` to add networking configuration fields to the ConfigMap data section:
   - `baseDomain: "example.com"` -- The base domain for DNS names (e.g., game.user.example.com)
   - `gatewayName: "kterodactyl-gateway"` -- Name of the Gateway resource HTTPRoutes attach to
   - `gatewayNamespace: "kterodactyl-system"` -- Namespace where the Gateway lives
   - `gatewayControllerNamespace: "envoy-gateway-system"` -- Namespace of the gateway controller data plane (for NetworkPolicy)
   - Add comments explaining each field's purpose
   - Keep all existing fields unchanged

3. Update `internal/controller/gameserver_controller.go` LoadAdminConfig function to parse the new fields. Add to AdminConfig struct:
   - `BaseDomain string`
   - `GatewayName string`
   - `GatewayNamespace string`
   - `GatewayControllerNamespace string`
   - Parse from ConfigMap data with defaults: BaseDomain="" (empty means DNS disabled), GatewayName="kterodactyl-gateway", GatewayNamespace="kterodactyl-system", GatewayControllerNamespace="envoy-gateway-system"

4. Run `go build ./...` to verify compilation.

Important: The AdminConfig struct and LoadAdminConfig live in gameserver_controller.go (from Phase 1). Update them in-place -- do not move to a separate file. The DNS controller in Plan 02 will import and use these.
  </action>
  <verify>
    `cd /home/tony/kterodactyl && go build ./...` succeeds.
    `grep "gatewayv1" cmd/main.go` shows the import.
    `grep "baseDomain" config/manager/admin-config.yaml` shows the new field.
    `grep "BaseDomain" internal/controller/gameserver_controller.go` shows the struct field.
  </verify>
  <done>
    Gateway API scheme registered in init(), admin ConfigMap has baseDomain/gatewayName/gatewayNamespace/gatewayControllerNamespace fields, AdminConfig struct and LoadAdminConfig updated with new fields and defaults, go build passes.
  </done>
</task>

</tasks>

<verification>
- `cd /home/tony/kterodactyl && go build ./...` compiles without errors
- `go vet ./...` passes
- Gateway API types importable: `grep "gateway-api" go.mod` returns v1.4.1
- `GameServerDNSName("minecraft", "alice", "example.com")` would return `"minecraft.alice.example.com"`
- AdminConfig.BaseDomain populated when ConfigMap has baseDomain key
</verification>

<success_criteria>
- Gateway API v1.4.1 dependency added to go.mod
- Gateway API scheme registered in cmd/main.go init()
- internal/util/networking.go exists with GameServerDNSName, AnnotationDNSName, LabelHTTPRouteOwner
- AdminConfig struct extended with BaseDomain, GatewayName, GatewayNamespace, GatewayControllerNamespace
- LoadAdminConfig parses new fields from ConfigMap with sensible defaults
- config/manager/admin-config.yaml documents all new networking fields
- Project compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-networking-dns/02-01-SUMMARY.md`
</output>
