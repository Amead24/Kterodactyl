---
phase: 12-documentation
plan: 02
type: execute
wave: 1
depends_on: ["12-01"]
files_modified:
  - docs-site/docs/usage/creating-servers.md
  - docs-site/docs/usage/managing-servers.md
  - docs-site/docs/usage/backups-restore.md
  - docs-site/docs/usage/admin-tasks.md
  - docs-site/docs/contributing/game-definitions.md
  - docs-site/docs/contributing/development.md
  - docs-site/docs/contributing/architecture.md
  - docs-site/docs/reference/api-endpoints.md
  - docs-site/docs/reference/crd-reference.md
  - docs-site/docs/reference/metrics.md
  - README.md
autonomous: true

must_haves:
  truths:
    - "Usage section walks users through creating, managing, backing up, and restoring game servers"
    - "Game definition contribution guide exists with Minecraft example walkthrough"
    - "Architecture overview with Mermaid diagrams explains system design for contributors"
    - "API reference documents all REST endpoints with methods, paths, auth requirements, and descriptions"
    - "CRD reference documents GameServer and Backup custom resource specs and status fields"
    - "Metrics reference lists all Prometheus metrics with labels and descriptions"
    - "README.md provides concise project overview and links to documentation site"
    - "Site builds successfully with all 15+ documentation pages"
  artifacts:
    - path: "docs-site/docs/contributing/game-definitions.md"
      provides: "Game definition contribution guide with Minecraft walkthrough"
      min_lines: 50
    - path: "docs-site/docs/contributing/architecture.md"
      provides: "Architecture overview with Mermaid diagrams"
      contains: "mermaid"
    - path: "docs-site/docs/reference/api-endpoints.md"
      provides: "REST API reference"
      min_lines: 40
    - path: "docs-site/docs/reference/crd-reference.md"
      provides: "CRD specification reference"
      contains: "GameServer"
    - path: "docs-site/docs/reference/metrics.md"
      provides: "Prometheus metrics reference"
      contains: "kterodactyl_"
    - path: "README.md"
      provides: "Updated project README"
      contains: "docs-site"
  key_links:
    - from: "docs-site/docs/contributing/game-definitions.md"
      to: "games/minecraft/manifest.yaml"
      via: "Minecraft reference example"
      pattern: "minecraft"
    - from: "docs-site/docs/contributing/architecture.md"
      to: "api/v1alpha1/gameserver_lifecycle.go"
      via: "State machine diagram"
      pattern: "stateDiagram"
    - from: "docs-site/docs/reference/api-endpoints.md"
      to: "internal/api/routes.go"
      via: "Route documentation"
      pattern: "/api/v1"
---

<objective>
Write the Usage, Contributing, and Reference documentation sections, and update the project README.

Purpose: Complete the documentation site with user workflow guides, contributor documentation (including game definition guide and architecture overview with Mermaid diagrams), API/CRD/metrics reference docs, and replace the Kubebuilder template README with a proper project overview.

Output: 10 new markdown documents completing all 5 sidebar categories, an updated README.md, and a fully buildable documentation site covering all 4 DOCS requirements.
</objective>

<execution_context>
@/home/tony/.claude/get-shit-done/workflows/execute-plan.md
@/home/tony/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-documentation/12-RESEARCH.md
@.planning/phases/12-documentation/12-01-SUMMARY.md
@docs/game-definitions.md
@games/minecraft/manifest.yaml
@api/v1alpha1/gameserver_types.go
@api/v1alpha1/backup_types.go
@api/v1alpha1/gameserver_lifecycle.go
@internal/api/routes.go
@internal/metrics/metrics.go
@Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Usage and Contributing documentation</name>
  <files>
    docs-site/docs/usage/creating-servers.md
    docs-site/docs/usage/managing-servers.md
    docs-site/docs/usage/backups-restore.md
    docs-site/docs/usage/admin-tasks.md
    docs-site/docs/contributing/game-definitions.md
    docs-site/docs/contributing/development.md
    docs-site/docs/contributing/architecture.md
  </files>
  <action>
    Write 7 markdown documentation files. Use code blocks for all angle brackets and curly braces (MDX strictness). Use Docusaurus admonitions where appropriate.

    **Usage (4 files):**

    1. **creating-servers.md**: Front matter with `sidebar_position: 1`. Walk through the user flow: log in, browse available games on the Games page, select a game (e.g., Minecraft), see the dynamic configuration form generated from the game's JSON Schema, fill in parameters (explain what EULA, TYPE, MAX_PLAYERS mean for Minecraft), click Create. Explain that the server enters Creating state and progresses through Starting to Ready. Describe how connection info appears once Ready (DNS name `game.username.baseDomain` and port).

    2. **managing-servers.md**: Front matter with `sidebar_position: 2`. Cover server lifecycle management: viewing server status on the dashboard, starting a stopped server, stopping a running server, restarting (goes through Creating again), deleting a server (permanent). Explain the 6 states (Creating, Starting, Ready, Allocated, Shutdown, Error) and what each means. Cover mod upload: navigating to Mods tab, drag-and-drop upload, server auto-restart after upload. Cover console: real-time log output via WebSocket, sending commands.

    3. **backups-restore.md**: Front matter with `sidebar_position: 3`. Explain on-demand backups: navigate to Backups tab on server detail, click Create Backup (server must be running). Describe backup status (Pending, InProgress, Completed, Failed). Explain that backups are stored in S3 with the server's backupPath. Cover scheduled backups (admin sets cron schedule). Cover restore (admin-only): select backup, confirm restore dialog. Note that restore replaces current data with backup contents.

    4. **admin-tasks.md**: Front matter with `sidebar_position: 4`. Cover admin workflows: inviting new users (create invite, share link or email), viewing and managing users (list, delete -- cannot delete self), setting backup schedules, monitoring via Prometheus metrics. Explain the admin role and how the first admin is bootstrapped via the API.

    **Contributing (3 files):**

    5. **game-definitions.md**: Front matter with `sidebar_position: 1`. Migrate and enhance content from docs/game-definitions.md. This is the DOCS-02 requirement. Structure: overview of the game definition system, directory structure (games/<name>/manifest.yaml + Dockerfile), complete manifest field reference table, parameter schema explanation with JSON Schema keywords (enum, pattern, const, maxLength, title, description, default, required), the full Minecraft manifest.yaml as a walkthrough example (read from games/minecraft/manifest.yaml), Dockerfile conventions (community images vs SteamCMD pattern), testing instructions (go test commands), and PR checklist. Make this a thorough guide that a contributor can follow to add a new game.

    6. **development.md**: Front matter with `sidebar_position: 2`. Local development setup: clone repo, install Go 1.24+, install Node.js 20+, install Helm. Document key Makefile targets: `make build` (full build with frontend), `make build-frontend` (frontend only), `make dev-frontend` (Vite dev server), `make test` (Go tests with envtest), `make docker-build` (container image), `make manifests` (regenerate CRDs), `make generate` (deepcopy methods), `make lint` (golangci-lint), `make install` (CRDs to cluster), `make deploy` (operator to cluster). Explain the project structure: api/ (CRD types), internal/controller/ (reconcilers), internal/api/ (REST API), internal/auth/ (authentication), internal/manifest/ (game loader), internal/metrics/ (Prometheus), web/ (React SPA), chart/ (Helm), games/ (game definitions), config/ (Kubebuilder config). Describe the multi-stage Dockerfile and embedded SPA pattern.

    7. **architecture.md**: Front matter with `sidebar_position: 3`. This is the DOCS-04 requirement. Include a Mermaid component diagram showing the manager binary containing GameServer Controller, DNS Controller, Backup Controller, and REST API with embedded SPA. Show how they connect to K8s API, Gateway API, S3 storage, and the user's browser. Include a Mermaid state diagram for the GameServer lifecycle (6 states with all valid transitions from gameserver_lifecycle.go). Include a Mermaid state diagram for the Backup lifecycle (4 states). Explain the dual-controller pattern (GameServer + DNS reconcilers watching same CRD). Explain the API design (Chi v5 router, JWT auth middleware, namespace-per-user isolation). Explain the game manifest system (directory-per-game, JSON Schema for validation and form generation). Reference the component diagram from 12-RESEARCH.md architecture section.

    After writing all files, verify build: `cd docs-site && npm run build`
  </action>
  <verify>
    `cd docs-site && npm run build` succeeds with no MDX errors. All 7 markdown files exist. `grep -c "mermaid" docs-site/docs/contributing/architecture.md` shows at least 2 Mermaid diagrams. `wc -l docs-site/docs/contributing/game-definitions.md` shows substantial content (100+ lines).
  </verify>
  <done>
    7 documentation pages exist covering Usage (4 guides) and Contributing (3 guides including game definition walkthrough and architecture with Mermaid diagrams). All pages render without MDX errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write Reference documentation and update README</name>
  <files>
    docs-site/docs/reference/api-endpoints.md
    docs-site/docs/reference/crd-reference.md
    docs-site/docs/reference/metrics.md
    README.md
  </files>
  <action>
    Write 3 reference documentation files and update the project README.

    **Reference (3 files):**

    1. **api-endpoints.md**: Front matter with `sidebar_position: 1`. Document all REST API endpoints from internal/api/routes.go. Organize by category:
       - Health: GET /healthz, GET /readyz (unauthenticated)
       - Auth: POST /api/v1/auth/login (public, 5/min rate limit), POST /api/v1/auth/register (public, 3/min rate limit), POST /api/v1/auth/refresh (authenticated)
       - Games: GET /api/v1/games (authenticated), GET /api/v1/games/{gameType} (authenticated)
       - GameServers: GET/POST /api/v1/gameservers (authenticated, create 10/min), GET/PUT/DELETE /api/v1/gameservers/{name} (authenticated), POST .../start, .../stop, .../restart (authenticated), GET .../metrics (authenticated)
       - Mods: POST/GET /api/v1/gameservers/{name}/mods (authenticated), DELETE .../mods/{filename} (authenticated)
       - Backups: POST/GET /api/v1/gameservers/{name}/backups (authenticated), DELETE /backups/{backupName} (admin), POST .../restore (admin), PUT .../backup-schedule (admin)
       - Admin: POST /api/v1/admin/invites (admin), GET /api/v1/admin/users (admin), DELETE /api/v1/admin/users/{username} (admin)
       - WebSocket: GET /api/v1/gameservers/{name}/console (JWT via query param)
       For each endpoint: method, path, auth requirement, rate limit if any, brief description. Use a table format per category.

    2. **crd-reference.md**: Front matter with `sidebar_position: 2`. Document both CRDs:
       - **GameServer**: Spec fields (gameType, image, resources, ports, parameters) with types, validation rules, and descriptions from gameserver_types.go. Status fields (state, address, ports, conditions). Print columns (Game, State, Address, Age). Short name: gs.
       - **Backup**: Spec fields (gameServerName, backupPaths) with descriptions from backup_types.go. Status fields (state, s3Key, s3Bucket, size, startedAt, completedAt, message, conditions). Print columns (GameServer, State, Size, Age). Short name: bk.
       Include example YAML for both CRDs (derive from config/samples/ patterns). Explain the status subresource.

    3. **metrics.md**: Front matter with `sidebar_position: 3`. Document all 5 Prometheus metrics from internal/metrics/metrics.go:
       - `kterodactyl_gameservers_by_state` (Gauge, labels: state, game_type) -- current count of GameServers by state
       - `kterodactyl_reconciliation_duration_seconds` (Histogram, labels: controller, buckets: 0.01-10s) -- reconciliation loop duration
       - `kterodactyl_http_requests_total` (Counter, labels: method, route, status_code) -- total HTTP requests
       - `kterodactyl_http_request_duration_seconds` (Histogram, labels: method, route, status_code, default buckets) -- request latency
       - `kterodactyl_http_requests_inflight` (Gauge, no labels) -- concurrent in-flight requests
       Explain that metrics are registered with controller-runtime registry and served on the manager metrics endpoint (port 8443). Include example PromQL queries for common monitoring scenarios (error rate, p99 latency, server count).

    **README.md update:**

    4. **README.md**: Replace the Kubebuilder template content entirely. Write a concise README with: project name and one-line description, 3-4 sentence overview paragraph (what it is, how it works, who it's for), key features bullet list (6-8 items), quick start section (helm install command, port-forward, bootstrap admin), link to full documentation (docs-site), link to game definitions contribution guide, project structure overview (brief -- point to docs for details), development section (make build, make test, make docker-build), license (Apache 2.0). Keep it under 100 lines -- the README should drive users to the docs site, not duplicate it.

    After writing all files, verify build: `cd docs-site && npm run build`
  </action>
  <verify>
    `cd docs-site && npm run build` succeeds. All 3 reference markdown files exist. `grep -c "kterodactyl_" docs-site/docs/reference/metrics.md` shows all 5 metric names. `grep "api/v1" docs-site/docs/reference/api-endpoints.md` confirms API routes documented. README.md no longer contains "TODO(user)" placeholders.
  </verify>
  <done>
    3 reference docs exist (API endpoints, CRD reference, metrics). README.md is updated with proper project overview and links to docs. Full documentation site builds with all 15+ pages across 5 categories. All 4 DOCS requirements are satisfied.
  </done>
</task>

</tasks>

<verification>
1. `cd docs-site && npm run build` completes without errors
2. Full page count: 15 documentation pages across 5 categories plus landing page
3. `ls docs-site/docs/usage/` shows 4 files
4. `ls docs-site/docs/contributing/` shows 3 files
5. `ls docs-site/docs/reference/` shows 3 files
6. `grep -c "mermaid" docs-site/docs/contributing/architecture.md` >= 2 (state machine + component diagram)
7. `grep "TODO" README.md` returns no results (template placeholders removed)
8. All DOCS requirements met: DOCS-01 (installation/config/usage), DOCS-02 (game definition guide), DOCS-03 (Helm values reference), DOCS-04 (architecture overview)
</verification>

<success_criteria>
- 4 Usage docs walk through creating servers, managing lifecycle, backups, and admin tasks
- Game definition contribution guide with full Minecraft example walkthrough (DOCS-02)
- Development guide with Makefile targets and project structure
- Architecture overview with Mermaid state machine and component diagrams (DOCS-04)
- API endpoint reference documents all routes with auth requirements
- CRD reference documents GameServer and Backup specs with examples
- Metrics reference documents all 5 Prometheus metrics with example PromQL
- README.md replaced with proper project overview linking to docs site
- Full site builds successfully with `npm run build`
</success_criteria>

<output>
After completion, create `.planning/phases/12-documentation/12-02-SUMMARY.md`
</output>
