---
phase: 05-game-definition-framework
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - internal/api/handlers_games.go
  - internal/api/handlers_gameserver.go
  - internal/api/handlers_games_test.go
  - internal/api/handlers_gameserver_test.go
  - internal/api/helpers_test.go
  - docs/game-definitions.md
  - Dockerfile
autonomous: true

must_haves:
  truths:
    - "API GET /api/v1/games and GET /api/v1/games/{gameType} responses include parameterSchema field"
    - "Creating a game server validates parameters against the game's JSON Schema before creating the K8s resource"
    - "Updating a game server validates merged parameters against the game's JSON Schema"
    - "Invalid parameters on create return 400 with descriptive validation error"
    - "Invalid parameters on update return 400 with descriptive validation error"
    - "Documentation explains how to contribute new game definitions via PR"
    - "Operator container image includes games/ directory in final stage"
  artifacts:
    - path: "internal/api/handlers_games.go"
      provides: "GameResponse with ParameterSchema field, updated gameManifestToResponse"
      contains: "ParameterSchema"
    - path: "internal/api/handlers_gameserver.go"
      provides: "Schema validation on create and update paths"
      contains: "ValidateParameters"
    - path: "internal/api/handlers_games_test.go"
      provides: "Tests verifying parameterSchema in game responses"
      contains: "ParameterSchema"
    - path: "internal/api/handlers_gameserver_test.go"
      provides: "Tests verifying schema validation rejects invalid params"
      contains: "validation"
    - path: "internal/api/helpers_test.go"
      provides: "Updated test manifest loader using directory-per-game structure with schema"
      contains: "parameterSchema"
    - path: "docs/game-definitions.md"
      provides: "Contribution guide for adding new game definitions"
      contains: "Contributing"
    - path: "Dockerfile"
      provides: "Updated Dockerfile copying games/ into final stage"
      contains: "COPY --from=builder /workspace/games"
  key_links:
    - from: "internal/api/handlers_games.go"
      to: "internal/manifest/manifest.go"
      via: "gameManifestToResponse reads ParameterSchema from GameManifest"
      pattern: "m\\.ParameterSchema"
    - from: "internal/api/handlers_gameserver.go"
      to: "internal/manifest/validate.go"
      via: "handleCreateGameServer and handleUpdateGameServer call ValidateParameters"
      pattern: "m\\.ValidateParameters"
    - from: "internal/api/helpers_test.go"
      to: "internal/manifest/manifest.go"
      via: "defaultTestManifestLoader creates directory-based test manifests with schema"
      pattern: "os\\.MkdirAll.*manifest\\.yaml"
    - from: "Dockerfile"
      to: "games/"
      via: "COPY --from=builder copies games directory to final stage"
      pattern: "COPY --from=builder /workspace/games"
---

<objective>
Integrate JSON Schema validation into the API layer and complete Phase 5 deliverables. Update game API responses to expose parameterSchema for frontend consumption. Add schema validation on both create and update GameServer paths. Update all tests for the new directory-based manifest loader. Write the contribution guide. Update the operator Dockerfile to include games/ in the final image.

Purpose: Completes GAME-02 (schema parameters exposed via API), GAME-04 (API serves schema for UI form generation), and GAME-05 (contribution documentation). Ensures the operator container image ships with game definitions.
Output: Fully functional schema-driven game definition framework with API integration, validation, tests, docs, and container packaging.
</objective>

<execution_context>
@/home/tony/.claude/get-shit-done/workflows/execute-plan.md
@/home/tony/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-game-definition-framework/05-RESEARCH.md
@.planning/phases/05-game-definition-framework/05-01-SUMMARY.md
@internal/api/handlers_games.go
@internal/api/handlers_gameserver.go
@internal/api/handlers_games_test.go
@internal/api/handlers_gameserver_test.go
@internal/api/helpers_test.go
@internal/manifest/manifest.go
@internal/manifest/validate.go
@Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add parameterSchema to API responses and schema validation to create/update handlers</name>
  <files>
    internal/api/handlers_games.go
    internal/api/handlers_gameserver.go
  </files>
  <action>
1. Update `internal/api/handlers_games.go`:
   - Add `ParameterSchema map[string]interface{} `json:"parameterSchema,omitempty"`` field to `GameResponse`.
   - Update `gameManifestToResponse` to set `ParameterSchema: m.ParameterSchema` from the manifest. This passes the raw schema through to the frontend for react-jsonschema-form consumption in Phase 6. Do not transform the schema.

2. Update `internal/api/handlers_gameserver.go`:
   - In `handleCreateGameServer`, after `parameters := mergeMaps(m.Parameters, req.Parameters)` and before creating the GameServer CR, add schema validation:
     ```
     if err := m.ValidateParameters(parameters); err != nil {
         respondError(w, http.StatusBadRequest, err.Error())
         return
     }
     ```
   - In `handleUpdateGameServer`, after `gs.Spec.Parameters = mergeMaps(gs.Spec.Parameters, req.Parameters)` and before calling `s.client.Update`, add schema validation. Need to look up the manifest first by gs.Spec.GameType:
     ```
     m, ok := s.manifestLoader.Get(gs.Spec.GameType)
     if ok {
         if err := m.ValidateParameters(gs.Spec.Parameters); err != nil {
             respondError(w, http.StatusBadRequest, err.Error())
             return
         }
     }
     ```
     If the manifest is not found (game definition removed after server creation), skip validation rather than blocking updates. This is defensive -- the update path should not break because an admin removed a game definition.

Do NOT change the Parameters type from `map[string]string`. Do NOT modify any other handler behavior.
  </action>
  <verify>
Run `cd /home/tony/kterodactyl && go build ./internal/api/...` -- compiles cleanly.
  </verify>
  <done>
GameResponse includes parameterSchema field. Create and update handlers validate parameters against schema. Invalid parameters return 400. Missing manifest on update path is handled gracefully (validation skipped).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update test infrastructure and API tests for schema-aware manifests</name>
  <files>
    internal/api/helpers_test.go
    internal/api/handlers_games_test.go
    internal/api/handlers_gameserver_test.go
  </files>
  <action>
1. Update `internal/api/helpers_test.go`:
   - Update `defaultTestManifestLoader` to use the directory-per-game structure. Instead of writing a flat `minecraft.yaml` file, create a `minecraft/` subdirectory in the temp dir and write `manifest.yaml` inside it.
   - Add a `parameterSchema` section to the test Minecraft manifest with at least 3 properties: EULA (const: "TRUE"), TYPE (enum: ["VANILLA", "PAPER"]), and MAX_PLAYERS (pattern: "^[1-9][0-9]*$"). Include `required: [EULA, TYPE]`. This ensures schema validation tests have constraints to test against.

2. Update `internal/api/handlers_games_test.go`:
   - In `TestHandleListGames`, add assertion that `mc.ParameterSchema` is not nil (the response includes schema).
   - In `TestHandleGetGame`, add assertion that `resp.ParameterSchema` is not nil and contains expected properties. Decode the response into a struct with `ParameterSchema map[string]interface{}` field.
   - Update the `GameResponse` decode target to include the `ParameterSchema` field if not already present.

3. Update `internal/api/handlers_gameserver_test.go`:
   - Add test `TestHandleCreateGameServer_InvalidParameters`: Create a game server with a parameter that violates the schema (e.g., TYPE: "INVALID_TYPE" which is not in the enum). Verify 400 status and error message containing "parameter validation failed".
   - Add test `TestHandleCreateGameServer_ValidParameters`: Create a game server with valid parameters that satisfy the schema (e.g., EULA: "TRUE", TYPE: "PAPER"). Verify 201 status and successful creation.
   - Add test `TestHandleUpdateGameServer_InvalidParameters`: Update an existing game server with a parameter violating the schema. Verify 400 status.
   - Ensure existing create/update tests still pass (they should -- default parameters from the manifest should satisfy the schema since defaults are designed to be valid).

Keep all existing tests intact. Only add new test cases and modify the test infrastructure.
  </action>
  <verify>
Run `cd /home/tony/kterodactyl && go test ./internal/api/... -v` -- all tests pass (both existing and new).
Run `cd /home/tony/kterodactyl && go test ./... -count=1` -- full test suite passes.
  </verify>
  <done>
Test manifest loader uses directory-per-game structure with schema. Game API tests verify parameterSchema in responses. GameServer create/update tests verify schema validation rejects invalid parameters and accepts valid ones. Full test suite passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create contribution guide and update operator Dockerfile</name>
  <files>
    docs/game-definitions.md
    Dockerfile
  </files>
  <action>
1. Create `docs/game-definitions.md` -- the contribution guide for GAME-05. Structure:

   # Contributing Game Definitions

   ## Overview
   Brief explanation: each game is a directory under `games/` with `manifest.yaml` and `Dockerfile`.

   ## Directory Structure
   Show the expected layout: `games/<gamename>/manifest.yaml` + `games/<gamename>/Dockerfile`.

   ## Manifest Format
   Document all fields:
   - `name` (required): unique identifier, lowercase, no spaces
   - `displayName` (required): human-readable name for UI
   - `image` (required): container image to run
   - `ports` (required): list of ports with name, containerPort, protocol
   - `parameters`: default key-value pairs (env vars)
   - `parameterSchema`: JSON Schema defining parameter constraints and UI metadata
   - `resources`: CPU/memory requests and limits

   ## Parameter Schema
   Explain how to define JSON Schema for parameters:
   - All parameter types are `string` (env vars are always strings)
   - Use `enum` for dropdown selections
   - Use `pattern` for numeric/formatted strings
   - Use `const` for fixed values
   - Use `maxLength` for text limits
   - Use `title` and `description` for UI labels and help text
   - Use `default` to document default values
   - Use `required` array for mandatory parameters

   Include a minimal example showing 2-3 parameters with different constraint types.

   ## Dockerfile
   Explain Dockerfile conventions:
   - Use existing community images when available (FROM image:latest)
   - Include documentation comments (what image, link to docs)
   - For custom images, show SteamCMD example pattern

   ## Testing Your Game Definition
   Show how to verify: `go test ./internal/manifest/... -v`
   Mention the manifest is loaded at startup -- errors surface immediately.

   ## PR Checklist
   - [ ] Directory created under `games/<gamename>/`
   - [ ] `manifest.yaml` has all required fields (name, displayName, image, ports)
   - [ ] `Dockerfile` exists with documentation comments
   - [ ] Container image is publicly accessible
   - [ ] `parameterSchema` validates correctly (if defined)
   - [ ] `go test ./internal/manifest/...` passes
   - [ ] Parameters use `type: string` (not integer, boolean, etc.)

   ## Reference
   Point to `games/minecraft/` as the reference implementation.

2. Update the root `Dockerfile`:
   - Add `COPY --from=builder /workspace/games /games` in the final stage, after the existing `COPY --from=builder /workspace/manager .` line. This ensures game definitions are included in the container image.
   - The games/ directory path `/games` in the container matches the default path the manifest loader expects.

Do NOT add a docs/ directory README or index. Just the single contribution guide file.
  </action>
  <verify>
Verify `docs/game-definitions.md` exists and contains sections: Overview, Directory Structure, Manifest Format, Parameter Schema, Dockerfile, Testing, PR Checklist, Reference.
Verify `Dockerfile` contains `COPY --from=builder /workspace/games /games`.
Run `cd /home/tony/kterodactyl && go build ./...` -- still compiles.
Run `cd /home/tony/kterodactyl && go test ./... -count=1` -- full test suite passes.
  </verify>
  <done>
Contribution guide covers all aspects of creating a new game definition. Operator Dockerfile includes games/ directory in final container image. Full project builds and all tests pass.
  </done>
</task>

</tasks>

<verification>
1. `go build ./...` compiles without errors
2. `go test ./... -count=1` -- all tests pass (manifest, API, controller)
3. `go vet ./...` reports no issues
4. `GET /api/v1/games` response includes `parameterSchema` field
5. `POST /api/v1/servers` with invalid params returns 400
6. `PATCH /api/v1/servers/{name}` with invalid params returns 400
7. `docs/game-definitions.md` exists with PR checklist
8. `Dockerfile` copies games/ into final stage
</verification>

<success_criteria>
- API responses expose parameterSchema for frontend consumption
- Schema validation prevents invalid parameter values on create and update
- All existing and new tests pass
- Contribution guide is complete with examples and PR checklist
- Operator container image includes game definitions
- Full project builds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-game-definition-framework/05-02-SUMMARY.md`
</output>
