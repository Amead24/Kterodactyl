---
phase: 13-go-test-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - internal/api/handlers_backups_test.go
autonomous: true
requirements:
  - GAPI-02

must_haves:
  truths:
    - "Backup create handler creates a Backup CR for a Ready GameServer and returns 201"
    - "Backup create handler returns 404 for non-existent server"
    - "Backup create handler returns 409 for non-running server"
    - "Backup create handler returns 409 when backup already in progress"
    - "Backup list handler returns sorted list of backups for a GameServer"
    - "Backup list handler returns 404 for non-existent server"
    - "Backup delete handler returns 204 and removes backup (admin only)"
    - "Backup delete handler returns 403 for non-admin user"
    - "Backup restore handler returns 404 for non-existent server"
    - "Backup restore handler returns 409 for non-completed backup"
    - "Backup restore handler returns 403 for non-admin user"
    - "Backup schedule handler sets cron annotation on GameServer (admin only)"
    - "Backup schedule handler returns 400 for invalid cron expression"
  artifacts:
    - path: "internal/api/handlers_backups_test.go"
      provides: "Tests for handleCreateBackup, handleListBackups, handleDeleteBackup, handleRestoreBackup, handleSetBackupSchedule"
      min_lines: 200
  key_links:
    - from: "internal/api/handlers_backups_test.go"
      to: "internal/api/helpers_test.go"
      via: "newTestServer, createTestGameServerWithState, createTestBackup"
      pattern: "createTestBackup"
    - from: "internal/api/handlers_backups_test.go"
      to: "internal/api/handlers_backups.go"
      via: "BackupResponse type and backupToResponse mapping"
      pattern: "BackupResponse"
---

<objective>
Write httptest-based tests for all five backup handler endpoints (create, list, delete, restore, schedule) covering happy paths and error cases including admin-only access control.

Purpose: Close the GAPI-02 coverage gap for the backup handler group, the largest untested handler set.
Output: New test file handlers_backups_test.go with passing tests.
</objective>

<execution_context>
@/home/tony/.claude/get-shit-done/workflows/execute-plan.md
@/home/tony/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-go-test-foundation/13-01-SUMMARY.md
@internal/api/handlers_backups.go
@internal/api/helpers_test.go
@internal/api/handlers_gameserver_test.go
@internal/api/routes.go
@internal/api/server.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write backup create and list handler tests</name>
  <files>internal/api/handlers_backups_test.go</files>
  <action>
Create `internal/api/handlers_backups_test.go` in `package api` following the established per-subtest fresh server pattern.

**Test functions to create:**

1. **TestHandleCreateBackup** with subtests:
   - `"creates backup for ready server"` (happy path) -- create Ready GameServer, POST /backups, assert 201. Decode `BackupResponse` and verify `GameServerName` matches and `State` is "Pending". Also verify the Backup CR was actually created in the fake client by listing backups with `client.MatchingLabels`.
   - `"server not found returns 404"` -- POST /backups for nonexistent server
   - `"server not running returns 409"` -- create Shutdown GameServer, POST /backups
   - `"backup already in progress returns 409"` -- create Ready GameServer, create a Backup CR with state `InProgress` using `createTestBackup`, then POST /backups

2. **TestHandleListBackups** with subtests:
   - `"returns empty list"` -- create Ready GameServer with no backups, GET /backups, assert 200 with count 0
   - `"returns sorted backups"` -- create Ready GameServer, create 2 Backup CRs with `createTestBackup` (one Completed, one Pending), GET /backups, assert 200 with count 2. Verify the response is a list (decode as `struct{ Data []BackupResponse; Count int }`).
   - `"server not found returns 404"` -- GET /backups for nonexistent server

**Route paths (from routes.go):**
- POST `/api/v1/gameservers/{name}/backups` -> handleCreateBackup
- GET `/api/v1/gameservers/{name}/backups` -> handleListBackups

**Implementation rules (per locked decisions):**
- Error case assertions use HTTP status codes ONLY
- Each `t.Run` gets its own `newTestServer(t)`
- Happy path assertions can decode and check response body fields (not error messages)
- Use `auth.RoleUser` tokens for create and list (these are user-accessible endpoints)
  </action>
  <verify>
Run `go test ./internal/api/... -v -run "TestHandleCreateBackup|TestHandleListBackups" -count=1` and verify all subtests pass.
  </verify>
  <done>
Backup create tests cover happy path (201 + CR verification), not-found, not-running, and in-progress-conflict. Backup list tests cover empty list, sorted list, and not-found. All pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write backup delete, restore, and schedule handler tests</name>
  <files>internal/api/handlers_backups_test.go</files>
  <action>
Append to `internal/api/handlers_backups_test.go` (same file as Task 1).

**Test functions to add:**

3. **TestHandleDeleteBackup** with subtests:
   - `"deletes backup as admin"` (happy path) -- create Ready GameServer, create Completed Backup with `createTestBackup`, DELETE /backups/{backupName} with admin token, assert 204. Verify backup is gone from fake client.
   - `"non-admin returns 403"` -- same setup but use `auth.RoleUser` token, assert 403
   - `"backup not found returns 404"` -- DELETE /backups/nonexistent with admin token
   - `"backup from different server returns 404"` -- create two GameServers, create backup for server A, try to delete via server B's route

4. **TestHandleRestoreBackup** with subtests:
   - `"server not found returns 404"` -- POST /backups/{backupName}/restore for nonexistent server with admin token
   - `"backup not found returns 404"` -- create Ready GameServer, POST /backups/nonexistent/restore with admin token
   - `"backup not completed returns 409"` -- create Ready GameServer, create Pending Backup, POST /restore with admin token
   - `"server not running returns 409"` -- create Shutdown GameServer, create Completed Backup, POST /restore with admin token
   - `"non-admin returns 403"` -- create Ready GameServer, create Completed Backup, POST /restore with user token
   Note: Cannot test the full restore happy path because it calls `loadAdminConfig` -> `createS3Client` -> `execInPod`. The validation paths (server not found, backup not found, backup not completed, server not running) are tested. The S3/exec path is integration scope.

5. **TestHandleSetBackupSchedule** with subtests:
   - `"sets schedule as admin"` (happy path) -- create Ready GameServer, PUT /backup-schedule with `{"schedule":"0 3 * * *","retention":5}` and admin token, assert 200. Decode response and verify schedule and retention fields.
   - `"removes schedule when empty"` -- create Ready GameServer with schedule annotation, PUT /backup-schedule with `{"schedule":""}` and admin token, assert 200
   - `"invalid cron returns 400"` -- create Ready GameServer, PUT /backup-schedule with `{"schedule":"invalid"}` and admin token
   - `"non-admin returns 403"` -- PUT /backup-schedule with user token
   - `"server not found returns 404"` -- PUT /backup-schedule for nonexistent server with admin token

**Route paths (from routes.go):**
- DELETE `/api/v1/gameservers/{name}/backups/{backupName}` -> handleDeleteBackup (admin-only via RequireAdmin middleware)
- POST `/api/v1/gameservers/{name}/backups/{backupName}/restore` -> handleRestoreBackup (admin-only)
- PUT `/api/v1/gameservers/{name}/backup-schedule` -> handleSetBackupSchedule (admin-only)

**Implementation rules:**
- Use `ts.generateToken(t, "admin", auth.RoleAdmin)` for admin endpoints
- Use `ts.generateToken(t, "alice", auth.RoleUser)` for 403 tests
- Error assertions use HTTP status codes only
- For schedule set/remove: construct JSON body using `json.Marshal(map[string]interface{}{...})` and pass as `bytes.NewReader`
- For schedule happy path: verify response body has correct schedule and retention values
  </action>
  <verify>
Run `go test ./internal/api/... -v -run "TestHandleDeleteBackup|TestHandleRestoreBackup|TestHandleSetBackupSchedule" -count=1` and verify all subtests pass.
  </verify>
  <done>
Backup delete tests cover admin happy path (204 + CR deletion), non-admin 403, not-found, cross-server isolation. Restore tests cover server-not-found, backup-not-found, not-completed, not-running, non-admin 403. Schedule tests cover set, remove, invalid cron, non-admin, not-found. All pass.
  </done>
</task>

</tasks>

<verification>
1. `go test ./internal/api/... -v -count=1` passes with ALL new and existing tests
2. handlers_backups_test.go covers all five backup endpoints
3. Admin-only endpoints (delete, restore, schedule) have 403 tests for non-admin users
4. Create happy path verifies Backup CR exists in fake client
5. No test asserts on error message text
6. Each subtest creates its own testServer
</verification>

<success_criteria>
- Backup create, list, delete, restore, and schedule handlers all have httptest-based tests
- Happy paths verify response body structure and K8s state
- Error paths verify HTTP status codes only
- Admin-only access control verified with 403 tests
- All tests pass against fake K8s client
</success_criteria>

<output>
After completion, create `.planning/phases/13-go-test-foundation/13-03-SUMMARY.md`
</output>
